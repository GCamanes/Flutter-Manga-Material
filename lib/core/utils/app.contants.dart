import 'package:flutter/material.dart';

abstract class AppConstants {
  /// UI constants
  // heights
  static const double settingsRowHeight = 50.0;
  static const double settingsSpacingHeight = settingsRowHeight / 2;

  // padding
  static const double horizontalPadding = NavigationToolbar.kMiddleSpacing;
}
